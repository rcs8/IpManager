import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AlocaçaoMonitoresComponent } from './alocaçao-monitores.component';

describe('AlocaçaoMonitoresComponent', () => {
  let component: AlocaçaoMonitoresComponent;
  let fixture: ComponentFixture<AlocaçaoMonitoresComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AlocaçaoMonitoresComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AlocaçaoMonitoresComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

/*   it('should create', () => {
    expect(component).toBeTruthy();
  });
  
  it('should allocate to a free assistant', (loginCin: string) => {
	  var monitor: Monitor();
	  var aluno: Aluno();
	  aluno.nome = loginCin
	  
	   
  });*/
  
  it('', () => {
	  
	  
  });
  
  it('', () => {
	  
	  
  });
  
  it('', () => {
	  
	  
  });
  
});
